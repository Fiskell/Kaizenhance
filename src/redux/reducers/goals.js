//import { Map } from 'immutable';
//
//function setState(state, newState) {
//  return state.merge(newState);
//}
//
//export default function goal(state = Map(), action) {
//  switch (action.type) {
//    case 'INIT':
//      return setState(state, action.state);
//
//    default:
//      return state
//  }
//}